interface Company {
    no: number;
    id: string;
    title: string;
    name: string;
    companyUrl: string;
    timePeriod: string;
    experienceDetails: Array<string>;
}

export default Company;
